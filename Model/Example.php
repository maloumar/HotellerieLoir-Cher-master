<?php

namespace Model;

use App\Db;

class NomClass extends Db
{

/*
    private $table = 'NomTable';
    private $column_id = 'NomColumnId';
*/

/*
    public function NomFonction(
        // Si création ou modification de données
        $datas
        // Si récupération d'un élément par son id ou sppression d'un élément par son id
        $id
    )
    {
        $sql = 'Requête SQL';
        $exec = $this->Db()->prepare($sql);
        $exec->execute(
            // Si $datas dans la fonction
            $datas
            // Si $id dans la fonction
            [':NomId'=>$id]
        );
        // Si cette fonction est relié à un affichage de données dans le controller
            // Si c'est un affichage de toute une table
            return $exec->fetchAll();
            // Sinon
            return $exec->fetch();
    }
*/

}

?>

e